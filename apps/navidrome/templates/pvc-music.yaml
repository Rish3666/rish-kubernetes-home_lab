apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: navidrome-music-pvc

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 100Gi

  storageClassName: manual
